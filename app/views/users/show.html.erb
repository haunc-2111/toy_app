<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<ul>
  <% @micropost.each do |m| %>
    <li>
      <p><%= m.content %></p>
      <%= link_to 'View', micropost_path(3) %>
    </li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>